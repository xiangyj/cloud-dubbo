spring:
  application:
    name: product-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: product
        namespace: ${spring.profiles.active}